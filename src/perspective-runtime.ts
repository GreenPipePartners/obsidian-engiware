// Compatibility entry point; implementation lives in the independent runtime.
export {
  PerspectiveRuntime, SampleTagStore, PERSPECTIVE_PROFILE, PERSPECTIVE_PROFILES,
  COMPONENT_TYPES, STYLE_PROPERTIES, object, text, scalarText, property, checkStyle,
} from '@greenpipepartners/engispark';
export type { Json, JsonObject, SampleTag, SampleValue, TagService, TagWriteResult } from '@greenpipepartners/engispark';
